# Phase X Handoff

> Copy to `designs/PHASE_X_HANDOFF.md` when the phase's exit criteria pass. Written for a brand-new
> agent picking up the NEXT phase with zero conversation context. Once this exists, the phase's
> `PHASE_X_CURRENT_STATUS.md` can be deleted.

**Phase:** X — <name> (bd epic: `<epic-id>`, closed YYYY-MM-DD)

## Phase summary
<2–4 sentences: what this phase delivered.>

## Exit criteria — evidence
<!-- Every exit criterion from designs/DESIGN.md, each with concrete pass evidence. -->
- <criterion> — PASS: <evidence, e.g. command output, test counts, platforms verified>
- README.md updated to reflect this phase (required for every phase) — PASS: <what changed>

## What changed
- <key directories/files added or reshaped, and their roles>

## How to verify
```bash
# exact commands a new agent can run to confirm the phase still works
```

## Decisions & deviations from DESIGN.md
- <decision + rationale; deviations must also be in DESIGN.md's deviation log>

## Deferred work
- `<bd-id>` <title> — <why deferred>

## Pointers for next phase
- <files/patterns the next phase builds on; traps to avoid>
